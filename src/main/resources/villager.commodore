villager {
  create {
    name brigadier:string greedy_phrase;
  }
  select;
  name {
    name brigadier:string greedy_phrase;
  }
  profession {
    profession brigadier:string single_word;
  }
  type {
    type brigadier:string single_word;
  }
  command {
    type brigadier:string single_word {
      command brigadier:string greedy_phrase;
    }
  }
  location;
  teleport;
}
